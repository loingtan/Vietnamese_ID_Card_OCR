apiVersion: v1
kind: Service
metadata:
  name: vnidcard-service
  labels:
    app: vnidcard-app
spec:
  type: LoadBalancer
  ports:
    - name: streamlit
      port: 8501
      targetPort: 8501
      protocol: TCP
    - name: api
      port: 8080
      targetPort: 8080
      protocol: TCP
  selector:
    app: vnidcard-app
---
apiVersion: v1
kind: Service
metadata:
  name: vnidcard-nodeport
  labels:
    app: vnidcard-app
spec:
  type: NodePort
  ports:
    - name: streamlit
      port: 8501
      targetPort: 8501
      nodePort: 30501
      protocol: TCP
    - name: api
      port: 8080
      targetPort: 8080
      nodePort: 30080
      protocol: TCP
  selector:
    app: vnidcard-app
